package org.live.is;

import static org.junit.Assert.*;

import org.junit.Test;

public class CalculoVerticesTest {

	@Test
	public void crearMatrizTest() {
		int columnas=3;
		int pisos=2;
		CalculoVertices vertices=new CalculoVertices(columnas,pisos);
		assertEquals(0,0);
	}

}
